// Code generated by goa, DO NOT EDIT.
//
// optimizer HTTP client CLI support package
//
// Command:
// $ goa gen github.com/aleksandarv/pack-optimizer/design -o
// /Users/aleksandarvukovic/projects/pack-optimizer

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	optimizerc "github.com/aleksandarv/pack-optimizer/gen/http/optimizer/client"
	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() []string {
	return []string{
		"optimizer (get-sizes|update-sizes|calculate)",
	}
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + " " + "optimizer get-sizes" + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, any, error) {
	var (
		optimizerFlags = flag.NewFlagSet("optimizer", flag.ContinueOnError)

		optimizerGetSizesFlags = flag.NewFlagSet("get-sizes", flag.ExitOnError)

		optimizerUpdateSizesFlags    = flag.NewFlagSet("update-sizes", flag.ExitOnError)
		optimizerUpdateSizesBodyFlag = optimizerUpdateSizesFlags.String("body", "REQUIRED", "")

		optimizerCalculateFlags    = flag.NewFlagSet("calculate", flag.ExitOnError)
		optimizerCalculateBodyFlag = optimizerCalculateFlags.String("body", "REQUIRED", "")
	)
	optimizerFlags.Usage = optimizerUsage
	optimizerGetSizesFlags.Usage = optimizerGetSizesUsage
	optimizerUpdateSizesFlags.Usage = optimizerUpdateSizesUsage
	optimizerCalculateFlags.Usage = optimizerCalculateUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "optimizer":
			svcf = optimizerFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "optimizer":
			switch epn {
			case "get-sizes":
				epf = optimizerGetSizesFlags

			case "update-sizes":
				epf = optimizerUpdateSizesFlags

			case "calculate":
				epf = optimizerCalculateFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "optimizer":
			c := optimizerc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "get-sizes":
				endpoint = c.GetSizes()
			case "update-sizes":
				endpoint = c.UpdateSizes()
				data, err = optimizerc.BuildUpdateSizesPayload(*optimizerUpdateSizesBodyFlag)
			case "calculate":
				endpoint = c.Calculate()
				data, err = optimizerc.BuildCalculatePayload(*optimizerCalculateBodyFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// optimizerUsage displays the usage of the optimizer command and its
// subcommands.
func optimizerUsage() {
	fmt.Fprintln(os.Stderr, `The optimizer service calculates optimal pack combinations for a given number of items.`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] optimizer COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    get-sizes: Get the current pack sizes.`)
	fmt.Fprintln(os.Stderr, `    update-sizes: Update the pack sizes.`)
	fmt.Fprintln(os.Stderr, `    calculate: Calculate optimal pack combinations for a given number of items.`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s optimizer COMMAND --help\n", os.Args[0])
}
func optimizerGetSizesUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] optimizer get-sizes", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Get the current pack sizes.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "optimizer get-sizes")
}

func optimizerUpdateSizesUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] optimizer update-sizes", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Update the pack sizes.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "optimizer update-sizes --body '{\n      \"sizes\": [\n         5187262674716634592,\n         7976492007773777393,\n         8733443723489356396\n      ]\n   }'")
}

func optimizerCalculateUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] optimizer calculate", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Calculate optimal pack combinations for a given number of items.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "optimizer calculate --body '{\n      \"totalItems\": 5521850667853442423\n   }'")
}
