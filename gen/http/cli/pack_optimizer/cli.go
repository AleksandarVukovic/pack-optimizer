// Code generated by goa, DO NOT EDIT.
//
// pack-optimizer HTTP client CLI support package
//
// Command:
// $ goa gen github.com/aleksandarv/pack-optimizer/design -o
// /Users/aleksandarvukovic/projects/pack-optimizer

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	optimizerc "github.com/aleksandarv/pack-optimizer/gen/http/optimizer/client"
	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() []string {
	return []string{
		"optimizer (health|get-pack-sizes|update-pack-sizes|calculate)",
	}
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + " " + "optimizer health" + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, any, error) {
	var (
		optimizerFlags = flag.NewFlagSet("optimizer", flag.ContinueOnError)

		optimizerHealthFlags = flag.NewFlagSet("health", flag.ExitOnError)

		optimizerGetPackSizesFlags = flag.NewFlagSet("get-pack-sizes", flag.ExitOnError)

		optimizerUpdatePackSizesFlags    = flag.NewFlagSet("update-pack-sizes", flag.ExitOnError)
		optimizerUpdatePackSizesBodyFlag = optimizerUpdatePackSizesFlags.String("body", "REQUIRED", "")

		optimizerCalculateFlags        = flag.NewFlagSet("calculate", flag.ExitOnError)
		optimizerCalculateQuantityFlag = optimizerCalculateFlags.String("quantity", "REQUIRED", "")
	)
	optimizerFlags.Usage = optimizerUsage
	optimizerHealthFlags.Usage = optimizerHealthUsage
	optimizerGetPackSizesFlags.Usage = optimizerGetPackSizesUsage
	optimizerUpdatePackSizesFlags.Usage = optimizerUpdatePackSizesUsage
	optimizerCalculateFlags.Usage = optimizerCalculateUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "optimizer":
			svcf = optimizerFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "optimizer":
			switch epn {
			case "health":
				epf = optimizerHealthFlags

			case "get-pack-sizes":
				epf = optimizerGetPackSizesFlags

			case "update-pack-sizes":
				epf = optimizerUpdatePackSizesFlags

			case "calculate":
				epf = optimizerCalculateFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "optimizer":
			c := optimizerc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "health":
				endpoint = c.Health()
			case "get-pack-sizes":
				endpoint = c.GetPackSizes()
			case "update-pack-sizes":
				endpoint = c.UpdatePackSizes()
				data, err = optimizerc.BuildUpdatePackSizesPayload(*optimizerUpdatePackSizesBodyFlag)
			case "calculate":
				endpoint = c.Calculate()
				data, err = optimizerc.BuildCalculatePayload(*optimizerCalculateQuantityFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// optimizerUsage displays the usage of the optimizer command and its
// subcommands.
func optimizerUsage() {
	fmt.Fprintln(os.Stderr, `Service calculates optimal pack combinations for a given number of items.`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] optimizer COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    health: Returns the health status of the service.`)
	fmt.Fprintln(os.Stderr, `    get-pack-sizes: Get the current pack sizes.`)
	fmt.Fprintln(os.Stderr, `    update-pack-sizes: Update the pack sizes.`)
	fmt.Fprintln(os.Stderr, `    calculate: Calculate optimal pack combinations for a given number of items.`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s optimizer COMMAND --help\n", os.Args[0])
}
func optimizerHealthUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] optimizer health", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Returns the health status of the service.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "optimizer health")
}

func optimizerGetPackSizesUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] optimizer get-pack-sizes", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Get the current pack sizes.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "optimizer get-pack-sizes")
}

func optimizerUpdatePackSizesUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] optimizer update-pack-sizes", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Update the pack sizes.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "optimizer update-pack-sizes --body '{\n      \"sizes\": [\n         250,\n         500,\n         1000\n      ]\n   }'")
}

func optimizerCalculateUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] optimizer calculate", os.Args[0])
	fmt.Fprint(os.Stderr, " -quantity INT")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Calculate optimal pack combinations for a given number of items.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -quantity INT: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "optimizer calculate --quantity 500")
}
